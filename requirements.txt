# SHL Assessment Recommendation System - Dependencies
# Python 3.10+ required

# ── Web framework ─────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0        # ASGI server with WebSocket + reload support

# ── Data validation ───────────────────────────────────────────
pydantic==2.7.1                  # v2 for field_validator syntax used in main.py

# ── LLM ───────────────────────────────────────────────────────
google-generativeai==0.7.2       # Gemini 1.5 Flash API

# ── RAG / Embeddings ──────────────────────────────────────────
sentence-transformers==3.0.1     # all-MiniLM-L6-v2 and others
faiss-cpu==1.13.2                 # CPU-only FAISS (Render uses CPU VMs)
                                 # Switch to faiss-gpu if using CUDA

# ── Data handling ─────────────────────────────────────────────
pandas==2.2.2
numpy==1.26.4                    # Must be <2.0 for faiss-cpu compatibility

# ── HTTP / Scraping ───────────────────────────────────────────
requests==2.32.2
beautifulsoup4==4.12.3
lxml==5.2.2                      # Faster HTML parser for BeautifulSoup

# ── Environment ───────────────────────────────────────────────
python-dotenv==1.0.1             # Loads .env file automatically

# ── Utilities ─────────────────────────────────────────────────
tqdm==4.66.4                     # Progress bars in scraper/embedding scripts
httpx==0.27.0                    # Async HTTP (used by FastAPI test client)

# ── Dev / Testing (optional, not required for production) ─────
# pytest==8.2.0
# pytest-asyncio==0.23.6
# httpx==0.27.0
